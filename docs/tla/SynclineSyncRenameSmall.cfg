\* Phase 5: Rename propagation — safety only (quick).

SPECIFICATION Spec

CONSTANTS
    Clients = {"A", "B"}
    PathNames = {"a.md", "b.md"}
    InitialPath = "a.md"
    MaxUpdates = 1
    MaxQueueLen = 2

CONSTRAINT
    StateConstraint

INVARIANTS
    NoContentLoss
    NoEcho
    ChannelOnlyForConnected
