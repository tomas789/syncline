\* Config for the FIXED diff layer — NoContentLoss should PASS.

SPECIFICATION Spec

CONSTANTS
    Clients = {"A", "B"}
    DocIds = {"d1"}
    MaxUpdates = 3
    MaxQueueLen = 3

CONSTRAINT
    StateConstraint

INVARIANTS
    NoEcho
    ChannelOnlyForConnected
    NoContentLoss
